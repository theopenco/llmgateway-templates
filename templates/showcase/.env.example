# The Showcase gallery is fully static — no API key required to run it.
# Set this only if you want the "Submit your app" button to open issues
# against your own fork instead of the upstream repo.
NEXT_PUBLIC_SHOWCASE_REPO=theopenco/llmgateway-templates
